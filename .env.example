# No Firebase or Gemini credentials are needed.
# Optional server settings (set environment variables, or use node --env-file=.env server.js):
PORT=3000
HOST=0.0.0.0
# DATA_FILE=./data/heartping.json
# Production: attach a persistent disk at /var/data and set:
# NODE_ENV=production
# DATA_FILE=/var/data/heartping.json
# Hosting providers supply PORT; do not override it with a development value.
